# Calculadora científica desarrollada en C++ usando Test-Driven Development (TDD)
## Funcionalidades
-Suma y resta de numeros reales
-Multiplicacion y division de numeros reales
-Raiz cuadrada (Metodo Newton-Ralphson)
-Funcion exponencial e^x (Serie de Taylor)

## Compilacion
```bash
g++ -o test_calc test_calc.cpp -std=c++11
g++ -o calc calc.cpp -std=c++11

#Ejecucion
```bash
./test_calc
./calc
```

